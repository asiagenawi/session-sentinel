---
description: Turn claude-powernap monitoring on
---

Run this command and show the user its output:

```
python3 "${CLAUDE_PLUGIN_ROOT}/src/claude_powernap/cli.py" on
```

(Use `python` if `python3` is not found.)
